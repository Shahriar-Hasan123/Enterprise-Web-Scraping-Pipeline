# scrapyd.conf
# Scrapyd server configuration
# Docs: https://scrapyd.readthedocs.io

[scrapyd]
# Bind to all interfaces inside Docker so we can reach it from outside
bind_address    = 0.0.0.0
http_port       = 6800

# Directories — all inside the container
eggs_dir        = eggs
logs_dir        = logs
dbs_dir         = dbs
items_dir       =
jobs_dir        =

# How many spiders can run at the same time
max_proc        = 4
max_proc_per_cpu = 4

# How many finished jobs to remember
finished_to_keep = 5
jobs_to_keep     = 5

[services]
schedule.json     = scrapyd.webservice.Schedule
cancel.json       = scrapyd.webservice.Cancel
status.json       = scrapyd.webservice.Status
addversion.json   = scrapyd.webservice.AddVersion
listprojects.json = scrapyd.webservice.ListProjects
listversions.json = scrapyd.webservice.ListVersions
listspiders.json  = scrapyd.webservice.ListSpiders
delproject.json   = scrapyd.webservice.DeleteProject
delversion.json   = scrapyd.webservice.DeleteVersion
listjobs.json     = scrapyd.webservice.ListJobs
daemonstatus.json = scrapyd.webservice.DaemonStatus